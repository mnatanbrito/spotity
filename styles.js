import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  withRedBorder: {
    borderWidth: 3,
    borderColor: 'red',
  },
  defaultFont: {
    fontFamily: 'Circular',
  }
});
